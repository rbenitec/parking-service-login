spring:
  application:
    name: login

  datasource:
    url: jdbc:mysql://localhost:3306/storex
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
    show-sql: true
config:
  api:
    utp:
      url: http://localhost:8120/interface/authenticate
server:
  port: 8110